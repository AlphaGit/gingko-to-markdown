'use strict';

module.exports = tagsToLinks;

var TAG_REGEX = /#[^\s#]+/g;

function tagsToLinks(content, options) {
	throw 'Not supported';
	// At the moment Gingko does not support deep linking to searches
}